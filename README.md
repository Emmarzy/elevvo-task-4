# Elevvo Internship - Task 4

## Data Requirements
- **Dataset:** `loan_approval_dataset.csv`
- **Expected local path:** place at workspace root so notebook paths resolve (for example `../../loan_approval_dataset.csv` from this task folder).

## Repository Data Policy
- Do not push large/raw datasets unless explicitly requested.
- Keep notebooks and documentation in Git; keep full raw data local.
- If needed, include only small sample rows for demonstration.

## Quick Setup
1. Download/obtain `loan_approval_dataset.csv`.
2. Place it in the project root (`Machine Learning Track/`).
3. Run `Loan_Approval_Prediction.ipynb`.
